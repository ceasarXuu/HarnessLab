# @ceasarxuu/harnesslab Transition Package

HarnessLab has been renamed to OrnnLab.

Use the active package and command instead:

```bash
npm install -g ornnlab
ornnlab --help
```

This scoped package keeps the old `harnesslab` command available only to point
users to the new `ornnlab` distribution path.
