# OrnnLab v0.1.4 Document Index

## Document Control

| Document Version | Engineering Version(s) | Updated | Change |
|---|---|---|---|
| 1.0 | OrnnLab Build Set (planned `2026.06.22`); `ornnlab` npm `0.1.4` (planned); Python app `0.2.0` | 2026-06-22 | Created the v0.1.4 document folder index to register in-flight work items before the version trio is approved. |
| 1.1 | OrnnLab Build Set (planned `2026.06.22`); `ornnlab` npm `0.1.4` (planned); Python app `0.2.0` | 2026-06-22 | Registered the second work item: `harnesslab-shim-retirement-prd.md` (HarnessLab compatibility layer + Rust legacy workspace immediate retirement, decided after the project owner confirmed no real legacy users exist). |
| 1.2 | OrnnLab Build Set (planned `2026.06.22`); `ornnlab` npm `0.1.4` (planned); Python app `0.2.0` | 2026-06-22 | Registered the implementation plan `harnesslab-shim-retirement-plan.md` derived from the PRD; status Draft pending OQ-1 sign-off (`integrations/terminal_bench/` rename vs. defer). |
| 1.3 | OrnnLab Build Set (planned `2026.06.22`); `ornnlab` npm `0.1.4` (planned); Python app `0.2.0` | 2026-06-22 | Grouped the three shim-retirement work-item documents under a dedicated `shim-retirement/` sub-folder for clarity; updated all governance, verify-script, and cross-document path references to the new location. |

This file indexes the active documentation bundle for OrnnLab `v0.1.4` during
the period before the version PRD / technical design / engineering plan trio is
cut. It follows the per-version folder rule defined in
`../v0.1.3/version-governance.md`.

## Status

- Version status: In planning. PRD / technical design / engineering plan trio
  not yet cut.
- Active work items: see "Work Items" below.
- Release ledger: not yet created. Will be added as
  `./ornnlab-0.1.4.md` when v0.1.4 is published.

## Work Items

| Document | Status | Purpose |
|---|---|---|
| `shim-retirement/harbor-rebrand-residue-fix-plan.md` | Phase 1–3 implemented; Phase 4 pending release ledger; shim-retirement open decision **closed 2026-06-22** | Close residual references to the pre-Harbor `docs/release/` documentation root and the legacy `HarnessLab` brand strings in user-visible WebUI surfaces. |
| `shim-retirement/harnesslab-shim-retirement-prd.md` | Implemented (see plan) | Immediately retire the HarnessLab compatibility layer (Python module shim, runtime data migration, environment variable fallbacks, Docker label / backup recognition, migration tests) and the Rust legacy workspace (`crates/`, `xtask/`, `Cargo.toml` and related tooling). npm transition package stays untouched. |
| `shim-retirement/harnesslab-shim-retirement-plan.md` | **Completed** (Phases 1–4 implemented and validated) | Standard 5-phase implementation plan derived from the PRD: Discovery → Python Shim Outer → Services/Settings/Tests → terminal_bench rename → Rust Workspace → Verification & Close-out. All 10 AC validated. |

## Planned Documents (not yet present)

The following documents are required before v0.1.4 can be cut. They are listed
here so that contributors and reviewers can see the gap at a glance. None of
them should be invented without explicit product confirmation.

| Document | Purpose | Source-of-truth chain |
|---|---|---|
| `../v0.1.4/prd.md` | Product requirements for v0.1.4 only. | Owns scope; must not be a total PRD. |
| `../v0.1.4/technical-design.md` | Technical design derived from the v0.1.4 PRD. | Cites `../v0.1.4/prd.md`. |
| `../v0.1.4/engineering-plan.md` | Implementation and validation plan for v0.1.4. | Cites both `../v0.1.4/prd.md` and `../v0.1.4/technical-design.md`. |
| `./ornnlab-0.1.4.md` | Release ledger binding npm launcher, Python app, frontend, transition package, Harbor range, and source commit. | Created at release-cut time. |

## Governance Rules That Apply

- `../v0.1.3/version-governance.md` rules carry over: per-version folder
  contract, `Document Control` table requirement, PRD document version
  metadata, and active-vs-archived distinction.
- Implementation evidence for already-shipped work items must be recorded in
  the corresponding work-item document, not duplicated here.
- When the v0.1.4 release ledger is published, this index must add the ledger
  link and update the status section.
